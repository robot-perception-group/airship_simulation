cmake_minimum_required(VERSION 2.8 FATAL_ERROR)

find_package(gazebo REQUIRED)
include_directories(${GAZEBO_INCLUDE_DIRS})
link_directories(${GAZEBO_LIBRARY_DIRS})
list(APPEND CMAKE_CXX_FLAGS "${GAZEBO_CXX_FLAGS}")

add_library(DynamicVolumePlugin SHARED DynamicVolumePlugin.cpp)
target_link_libraries(DynamicVolumePlugin ${GAZEBO_LIBRARIES})

#include sdf library
include_directories(${/usr/include/sdformat-6.2/sdf})

#include sdf library
include_directories(${/usr/include/Eigen})
